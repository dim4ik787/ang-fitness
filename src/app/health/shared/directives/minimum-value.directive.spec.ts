import { MinimumValueDirective } from './minimum-value.directive';

describe('MinimumValueDirective', () => {
  it('should create an instance', () => {
    const directive = new MinimumValueDirective();
    expect(directive).toBeTruthy();
  });
});
